import { Formik, Form, Field } from "formik";
import { Form } from "react-router-dom";

const MoviesPage = () => {
  return (
    <Formik>
      <Form>
        <Field></Field>
      </Form>
    </Formik>
  );
};

export default MoviesPage;
